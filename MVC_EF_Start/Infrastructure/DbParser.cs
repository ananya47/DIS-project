using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using MVC_EF_Start.Models;

namespace MVC_EF_Start.Infrastructure
{
    public class DbParser
    {
        public static void boatsParser(Boat boats)
        {

            
           /* boats.ForEach(p =>
            {
                Boat boat = new Boat()
                {
                    type = p.type,
                    company = p.company,
                    street_address = p.street_address
                };
                db.Boats.Add(boat);
                db.SaveChanges();
            });*/
           /* Boats obj1 = new Boats();
            type = obj1.type();*/
            throw new NotImplementedException();
        }
    }
}
